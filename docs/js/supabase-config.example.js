// คัดลอกไฟล์นี้เป็น supabase-config.js แล้วใส่ค่าจริงจาก Supabase Dashboard
// อย่า commit รหัสผ่าน Database — ใส่เฉพาะ URL และ anon key (public key)

window.SUPABASE_CONFIG = {
  enabled: true,
  url: 'https://xxxxxxxx.supabase.co',
  anonKey: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'
};
